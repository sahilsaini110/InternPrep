# name = input("name pls");
print("hello", "name");
# print(*objects, sep=' ', end='\n', file= sys.stdout, flus=False);

# *Objects means print can take any number or arguments can be take
# sep =' ' means if we pass more than one argument by default they provide one space.
 